
#### 7) Create a README file to answer the following question-


#### 1) What is the difference between var, let, and const?
Answer:

var: var is oldest way of declaring variable.function scoped,variable can be re-declared.
let: let is newest way of declaring variable.blocked scoped, variable can be re-declared.
const : const is also newest of declaring variable.blocked scoped , variable  can not be re-declared.









#### 2) What is the difference between map(), forEach(), and filter()?
Answer:
map(): oldest array convert to new array. every items of array transformed to new. it returns
forEach(): it does not give new array. it  doesn't return anything.
filter(): if condition true then item return, and making new array with selected  items

#### 3) What are arrow functions in ES6?
Answer:

arrow function: arrow function is function which is a new and short syntax of modern javascript.

#### 4) How does destructuring assignment work in ES6?

Answer:destructuring means extracting values from an object or an array and assignment them to small variables.

how works in assignment:
const showOnCart =()=>{
    const {name, price,id} = plant;
    alert(`${name} has been added to the cart`);
   div.innerHtml=
   `<div id="cart-${id}">
    <h3>${name}</h3>
    <p> ${price}</p>
    </div>`;

}
#### 5) Explain template literals in ES6. How are they different from string concatenation?

